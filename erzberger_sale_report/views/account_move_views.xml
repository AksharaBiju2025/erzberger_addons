<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_move_form_invoice_print_btn" model="ir.ui.view">
        <field name="name">account.move.form.custom.invoice.button</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">

            <xpath expr="//header/button[@name='action_print_pdf'][1]" position="replace">
                <button
                    string="Print"
                    type="action"
                    name="%(erzberger_sale_report.action_report_invoice_custom)d"
                    icon="fa-print"
                    invisible="move_type not in ('out_invoice','out_refund') or state != 'posted'"
                />
            </xpath>
            <xpath expr="//header/button[@name='action_print_pdf']" position="replace"/>

        </field>
    </record>

</odoo>