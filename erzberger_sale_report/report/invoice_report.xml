<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="action_report_invoice_custom" model="ir.actions.report">
        <field name="name">Invoice</field>
        <field name="model">account.move</field>
        <field name="report_type">qweb-pdf</field>

        <field name="report_name">erzberger_sale_report.report_invoice_document_custom</field>
        <field name="report_file">erzberger_sale_report.report_invoice_document_custom</field>

        <field name="print_report_name">'Invoice - %s' % (object.name)</field>

        <field name="binding_model_id" ref="account.model_account_move"/>
        <field name="binding_type">report</field>
        <field name="paperformat_id"
           ref="erzberger_sale_report.paperformat_sale_order_confirmation"/>
    </record>

</odoo>